<p>
<strong><%= comment.user.email %></strong> says: 
<%= comment.content %>
</p>

<% if comment.user == current_user %>
  <nav>
  <%= link_to 'Edit', edit_photo_comment_path(@photo, comment) %>
  <%= link_to 'Delete comment', [photo, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
  </nav>
<% end %>

